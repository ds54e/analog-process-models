# SPDX-FileCopyrightText: 2026 APM contributors
# SPDX-License-Identifier: Apache-2.0
schema = "apm.research-confirmation-plan.v1"
id = "v5-confirmation-1"
profile = "variation/research/apm045/derived/hart_tsmc40_profile.json"
artificial_profile = "tests/fixtures/research/artificial.json"
polarity = ["n", "p"]
w_um = [1.0, 2.0, 4.0]
l_um = [0.12, 0.24, 0.40]
holdouts_um = [[1.5, 0.18], [3.0, 0.32]]
reference_temperature_k = 300.0
reference_vds_v = 0.05
mapping_step_v = 0.001
mapping_fine_step_v = 0.0005
statistics_step_v = 0.002
pure_pairs = 65536
spice_pairs_per_geometry_polarity = 4096
circuit_realizations_per_family = 1024
sampler_seed = 2026090501
mapping_seed = 2026090502
sample_index_start = 1000000
development_cohort_note = "Development used indices 0-7 (statistics) and 0-3 (circuits) of the declared seeds; confirmation starts at 1000000, without searching seeds or reusing those draws."
statistics_seed = 2026090503
circuits_seed = 2026090504
bootstrap_seed = 2026090505
ratio_bootstrap_replicates = 8192
workers = 4
latent_limit = 6.0
campaign_scalar_draws = 204800
campaign_risk_max = 0.001
condition_max = 100.0
jacobian_half_step_relative_max = 0.02
extraction_over_sigma_max = 0.005
mapping_over_sigma_max = 0.02
zero_absolute_floors = [1e-8, 1e-6]
pure_sigma_equivalence = [0.98, 1.02]
spice_sigma_equivalence = [0.90, 1.10]
simultaneous_alpha = 0.05
primary_statistics = "Vth difference and beta difference/population nominal; chi-square sigma intervals with Bonferroni over 44 marginals; both endpoints must lie inside equivalence band. Mean bound 0.1 pair sigma and correlation bound 0.1."
nonlinear_ratio = "Actual pair-average beta ratio; deterministic bootstrap 95% interval and compare its standard deviation to ratio of the persisted target beta values. Report deviation from linear population normalization separately; no assumed global Gaussian-ratio moment."
cross_bias_gm_id = [5.0, 10.0, 15.0]
cross_bias_vds_v = [0.05, 0.5]
cross_bias_sigma_relative_band = [0.8, 1.2]
cross_bias_acceptance = "Actual-sensitivity prediction valid at at least two levels per polarity; simplify-Croon deviation is reported, not used to refit source inputs. No sampled gate retuning."
circuit_sigma_relative_band = [0.8, 1.2]
circuit_families = ["mirror1", "mirror4", "diffpair", "bank1", "bank4", "bank16"]
circuit_geometry_um = [1.0, 0.24]
replay_temperature_c = [-40.0, 27.0, 85.0, 125.0]
ac_tran_amplitude_relative_max = 0.02
ac_tran_phase_difference_rad_max = 0.03
io_lengths_um = [0.5, 1.0, 1.5, 2.0]
io_widths_um = [2.0, 4.0]
io_frequency_hz = [1000.0, 1000000.0]
io_bias_fraction = [0.6, 0.8, 1.0]
io_proxy_fit_relative_max = 0.05
failure_policy = "Retain every requested sample and failure; no replacement seeds, deletion, clipping or redraw. Revisions require a new committed plan and fresh cohort, retaining this plan and results."
