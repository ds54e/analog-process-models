# Codex: APM v6 repository redesign

This file is an inert routing prompt until an explicit user message selects the
v6 mission. It does not by itself supersede the current `GOAL.md`.

When the user explicitly authorizes this v6 task, work on current `main` of
`ds54e/analog-process-models` and execute the mission autonomously.

Read, in this order:

1. `AGENTS.md`;
2. current `GOAL.md` and `STATUS.md`;
3. `docs/maintainers/v6-plan.md` in full;
4. current `README.md`, `ENVIRONMENT.md`, `CONTRIBUTING.md`;
5. current validation/runtime code and tests named by the v6 plan;
6. all relevant frozen selectors, release identities, source/profile contracts,
   licenses and provenance before moving or deleting anything.

The explicit user selection of this task authorizes the policy transition described
in `docs/maintainers/v6-plan.md`. First migrate mutable `GOAL.md`, current mission
routing, version/lifecycle checks and any directly coupled current policy coherently.
Do not interpret the pre-v6 post-v5 maintenance wording as a reason to stop after
the user has explicitly selected this v6 task. Historical tags, commits, evidence,
scientific source decisions and acceptance criteria remain immutable.

This is full implementation and qualification work, not another preflight,
architecture proposal, documentation-only pass, or partial refactor.

Preserve APM v5 scientific meaning and all runtime-required scientific assets.
Do not add new device physics, statistical coefficients, Research Global/All,
new nominal models, IO statistical profiles, LDO/OTA optimization, noise Monte
Carlo, real Spectre claims, or an agent/plugin framework.

Implement the repository/documentation/validation/history redesign in the v6 plan,
including dependency tracing, verified history migration, current-runtime separation
from historical release implementations, executable user journeys, fail-closed
negative controls, old/new scientific compatibility checks, and the full required
v6 confirmation campaign.

Before retiring any live historical copy, prove exact reconstruction/export from
its documented Git source/evidence authority and preserve every current runtime or
license dependency. Do not hide failures by weakening gates, changing hashes,
rescaling source data, relaxing tolerances after results, discarding samples,
clipping/redrawing variation, or rewriting history.

Use normal fast-forward Git history. Commit and push coherent progress. Do not
force-push, move/recreate released tags, alter repository security/visibility, or
replace user system tools.

Continue through all independent work and all feasible required candidate gates.
Do not stop at an intermediate milestone. If a genuine scientific or infrastructure
blocker remains, complete everything independent of it and document the blocker
precisely with retained evidence.

Freeze and independently qualify one exact `6.0.0` candidate from an authoritative
fresh clone. Stop only at `V6_RELEASE_READY`, reporting the exact candidate commit,
tree, gate results, evidence, migration inventory, compatibility results and
remaining limitations.

Do not create or publish `v6.0.0`. Tag creation, exact-tag requalification on the
real project tag, and GitHub Release publication require separate user approval.
